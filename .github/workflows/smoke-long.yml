name: incuse smoke (long)
on: workflow_dispatch
jobs:
  hello:
    runs-on: incuse-4vcpu-8gb-20gb-amd64
    steps:
      - run: uname -a; cat /etc/os-release
      - run: sleep 60
      - run: echo done
